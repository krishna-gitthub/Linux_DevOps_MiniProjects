# Fstab Mount Validator

## Scenario
Checks if all mounts in /etc/fstab are valid and logs the result.

## Script
Filename: `fstab_mount_validator.sh`

## How to Run
```bash
bash fstab_mount_validator.sh
```